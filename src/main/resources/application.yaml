
# Set the active profile here
spring:
  profiles:
    active: prod

debug: false

management.endpoints.web.exposure.include: "*"

api:
  key: ${API_KEY}
com:
  example:
    upload:
      path:
        profileImg: "/server/upload/profileImg/"